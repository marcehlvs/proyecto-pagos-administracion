diff --git a/pagos-administracion-mvc/Controllers/AsistenciasController.cs b/pagos-administracion-mvc/Controllers/AsistenciasController.cs
index fdf4110..7f590b6 100644
--- a/pagos-administracion-mvc/Controllers/AsistenciasController.cs
+++ b/pagos-administracion-mvc/Controllers/AsistenciasController.cs
@@ -210,5 +210,58 @@ namespace pagos_administracion_mvc.Controllers
             ViewBag.Curso = curso;
             return View(filas);
         }
+
+        // GET: Asistencias/Grilla?cursoId=1&mes=9&anio=2026
+        // Un alumno por fila, un día hábil del mes por columna — para detectar de un vistazo qué
+        // días quedaron sin cargar (celda vacía), sin tener que entrar fecha por fecha a Tomar.
+        // Solo mira Materia.Clase (no Educación Física) para que la grilla quede legible.
+        public async Task<IActionResult> Grilla(int cursoId, int? mes, int? anio)
+        {
+            var (curso, error) = await ObtenerCursoConPermiso(cursoId);
+            if (error != null) return error;
+
+            var hoy = DateTime.Today;
+            var mesElegido = mes ?? hoy.Month;
+            var anioElegido = anio ?? hoy.Year;
+
+            var feriados = await _context.Feriados.Select(f => f.Fecha.Date).ToListAsync();
+            var diasHabiles = DiasHabilesDelMes(mesElegido, anioElegido, feriados);
+
+            var inscripciones = await _context.Inscripciones
+                .Include(i => i.Alumno)
+                .Include(i => i.Asistencias.Where(a => a.Materia == Materia.Clase &&
+                                                        a.Fecha.Month == mesElegido && a.Fecha.Year == anioElegido))
+                .Where(i => i.CursoId == cursoId)
+                .OrderBy(i => i.Alumno.Apellido)
+                .ToListAsync();
+
+            var filas = inscripciones.Select(i => new FilaAlumnoGrilla
+            {
+                AlumnoNombre = $"{i.Alumno.Apellido}, {i.Alumno.Nombre}",
+                EstadosPorDia = diasHabiles.ToDictionary(
+                    d => d,
+                    d => i.Asistencias.FirstOrDefault(a => a.Fecha.Date == d)?.Estado)
+            }).ToList();
+
+            ViewBag.Curso = curso;
+            ViewBag.Mes = mesElegido;
+            ViewBag.Anio = anioElegido;
+            return View(new GrillaAsistenciaViewModel { DiasHabiles = diasHabiles, Filas = filas });
+        }
+
+        // Mismo criterio de "día hábil" que BoletinService.ContarDiasHabiles (sábados, domingos
+        // y Feriados afuera), pero devolviendo la lista de fechas en vez de solo el total.
+        private static List<DateTime> DiasHabilesDelMes(int mes, int anio, ICollection<DateTime> feriados)
+        {
+            var dias = new List<DateTime>();
+            var ultimoDia = new DateTime(anio, mes, 1).AddMonths(1).AddDays(-1);
+            for (var d = new DateTime(anio, mes, 1); d <= ultimoDia; d = d.AddDays(1))
+            {
+                if (d.DayOfWeek == DayOfWeek.Saturday || d.DayOfWeek == DayOfWeek.Sunday) continue;
+                if (feriados.Contains(d.Date)) continue;
+                dias.Add(d);
+            }
+            return dias;
+        }
     }
 }
\ No newline at end of file
diff --git a/pagos-administracion-mvc/Models/AsistenciasResumenViewModels.cs b/pagos-administracion-mvc/Models/AsistenciasResumenViewModels.cs
index c2e5a5c..849519d 100644
--- a/pagos-administracion-mvc/Models/AsistenciasResumenViewModels.cs
+++ b/pagos-administracion-mvc/Models/AsistenciasResumenViewModels.cs
@@ -1,3 +1,5 @@
+using static pagos_administracion_mvc.Models.Enums;
+
 namespace pagos_administracion_mvc.Models
 {
     // Una fila del ranking general: un alumno + curso + su total de faltas acumulado.
@@ -29,4 +31,19 @@ namespace pagos_administracion_mvc.Models
         // (Admin: todos los cursos activos. Docente: solo los suyos).
         public List<FilaAlumnoFaltas> RankingGeneral { get; set; } = new();
     }
+
+    // Una fila de la grilla Alumno × día hábil del mes (Asistencias/Grilla). El valor es null
+    // cuando ese día todavía no tiene asistencia cargada para ese alumno — es justamente lo que
+    // la grilla existe para mostrar de un vistazo, sin entrar día por día a Tomar.
+    public class FilaAlumnoGrilla
+    {
+        public string AlumnoNombre { get; set; } = string.Empty;
+        public Dictionary<DateTime, EstadoAsistencia?> EstadosPorDia { get; set; } = new();
+    }
+
+    public class GrillaAsistenciaViewModel
+    {
+        public List<DateTime> DiasHabiles { get; set; } = new();
+        public List<FilaAlumnoGrilla> Filas { get; set; } = new();
+    }
 }
diff --git a/pagos-administracion-mvc/Views/Asistencias/ResumenCurso.cshtml b/pagos-administracion-mvc/Views/Asistencias/ResumenCurso.cshtml
index aa38386..4c8e028 100644
--- a/pagos-administracion-mvc/Views/Asistencias/ResumenCurso.cshtml
+++ b/pagos-administracion-mvc/Views/Asistencias/ResumenCurso.cshtml
@@ -5,7 +5,8 @@
 }
 
 <h1 class="mb-1">Faltas — @curso.Etiqueta</h1>
-<p class="text-muted mb-4">@Model.Count alumno(s) inscripto(s).</p>
+<p class="text-muted mb-2">@Model.Count alumno(s) inscripto(s).</p>
+<p class="mb-4"><a asp-action="Grilla" asp-route-cursoId="@curso.Id">Ver grilla día por día →</a></p>
 
 @if (!Model.Any())
 {
